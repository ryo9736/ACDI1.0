<% @posts.each do |post| %>
    <p>
      <strong>name:</strong>
      <%= post.user.name %>
    </p>
    <p>
      <strong>title:</strong>
      <%= post.title %>
    </p>
    <P>
    <%= link_to 'show',post_path(post.id)%>
    </P>
  <% end %>